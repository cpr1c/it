
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Задача) Тогда
		
		ДатаОбновленияЗадачи = Задача.ДатаОбновления;
		Если ДатаОбновления > ДатаОбновленияЗадачи Тогда
			ЗадачаОбъект = Задача.ПолучитьОбъект();
			ЗадачаОбъект.ДатаОбновления = ДатаОбновления;
			ЗадачаОбъект.Записать();
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры
